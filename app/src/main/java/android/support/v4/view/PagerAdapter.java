package android.support.v4.view;

import androidx.viewpager.widget.PagerAdapter;

public abstract class PagerAdapter extends PagerAdapter {
    // This class intentionally extends the AndroidX PagerAdapter to provide
    // a binary-compatible shim for code still importing android.support.v4.view.PagerAdapter.
}
